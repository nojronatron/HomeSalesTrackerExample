<UserControl x:Class="HomeSalesTrackerApp.CrudWindows.AddHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomeSalesTrackerApp.CrudWindows"
             xmlns:data="clr-namespace:HomeSalesTrackerApp.DisplayModels"
             mc:Ignorable="d" 
             >

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type data:HomeDisplayModel}" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="5" />
        </Grid.ColumnDefinitions>
            <Grid x:Name="HomeItems" Grid.Column="1" >
            <StackPanel Orientation="Vertical" MinWidth="130" Margin="5" Background="Azure">
                <Label Content="Address"  />
                <TextBlock x:Name="Address" Text="{Binding Path=Address}" />
                <Label Content="City" />
                <TextBlock x:Name="City" Text="{Binding Path=City}" />
                <Label Content="State" />
                <TextBlock x:Name="State" Text="{Binding Path=State}" />
                <Label Content="Zip Code" />
                <TextBlock x:Name="Zip" Text="{Binding Path=Zip}" />
                <Button x:Name="SaveHome" Content="Save New Home" Margin="5" />
            </StackPanel>
        </Grid>
        <Grid x:Name="Owners" Grid.Column="3">
            <StackPanel Orientation="Vertical" MinWidth="130" Margin="10" Background="Azure">
                <Label Content="Select an Existing Owner" />
                <ComboBox x:Name="OwnersList" ItemsSource="{Binding}" />
                <Label Content="Add new Owner person" />
                <Button x:Name="AddNewOwner" Content="Add New Owner" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
